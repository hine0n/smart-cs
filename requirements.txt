# LangChain 核心（下限已抬高到当前 venv 锁定版本，避免未来破坏性大版本误入）
langchain>=1.3.0
langchain-core>=1.4.0
langchain-openai>=1.3.0
langchain-community>=0.4.0
langchain-text-splitters>=1.1.0
langchain-chroma>=1.1.0

# 向量数据库
chromadb>=1.5.0

# Web 界面
streamlit>=1.59.0

# 文档解析
pypdf>=6.14.0
docx2txt>=0.9
pandas>=3.0.0
openpyxl>=3.1.5

# 环境变量
python-dotenv>=1.2.0

# 知识中台后端 (FastAPI) —— 前后端分离 / 多应用接入
fastapi>=0.139.0
uvicorn[standard]>=0.51.0
python-multipart>=0.0.32
requests>=2.34.0

# 重排序 cross-encoder 模式（可选，默认 none 模式关闭重排序；启用 llm/cross-encoder 均无需额外 pip 依赖，cross-encoder 需联网下载模型）
# sentence-transformers>=2.0.0
